# VegasCore Architecture - Post-Overhaul

## System Overview

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          VEGASCORE PLATFORM                              │
│                     (Node.js + Socket.IO + Prisma)                      │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                    ┌───────────────┴───────────────┐
                    │                               │
            ┌───────▼────────┐            ┌────────▼────────┐
            │  HTTP Layer    │            │  Socket.IO      │
            │  (Express)     │            │  (Real-time)    │
            └───────┬────────┘            └────────┬────────┘
                    │                               │
        ────────────┴───────────────────────────────┴─────────────
                    │                               │
        ┌───────────▼──────────┐      ┌────────────▼───────────┐
        │  REST API Endpoints  │      │  Socket Event Handlers │
        ├──────────────────────┤      ├────────────────────────┤
        │ • /api/casino-status │      │ • join_bingo_hall      │
        │ • /api/admin/users   │      │ • war:placeBet         │
        │ • /auth/webauthn/*   │      │ • blackjack:action     │
        │ • /me                │      │ • let_it_ride:bet      │
        └──────────┬───────────┘      └────────────┬───────────┘
                   │                               │
                   └───────────┬───────────────────┘
                               │
                   ┌───────────▼───────────┐
                   │   Core Services       │
                   ├───────────────────────┤
                   │ • AutoModeration      │
                   │ • EngagementService   │
                   │ • SyndicateService    │
                   │ • ReferralService     │
                   │ • GenerosityService   │
                   └───────────┬───────────┘
                               │
            ┌──────────────────┼──────────────────┐
            │                  │                  │
    ┌───────▼────────┐ ┌──────▼───────┐ ┌───────▼────────┐
    │  Game Engines  │ │   Database   │ │  Redis Cache   │
    ├────────────────┤ │   (Prisma)   │ │  (Sessions)    │
    │ • WarEngine    │ │              │ │                │
    │ • BlackjackEng │ │  PostgreSQL  │ │  Upstash/Local │
    │ • BingoEngine  │ │              │ │                │
    │ • LetItRideEng │ │              │ │                │
    └────────────────┘ └──────────────┘ └────────────────┘
```

---

## Phase I: Security & Temporal Authority

### Middleware Stack (server.js)
```
┌─────────────────────────────────┐
│  Incoming HTTP Request          │
└────────────┬────────────────────┘
             │
    ┌────────▼────────┐
    │  Trust Proxy    │
    └────────┬────────┘
             │
    ┌────────▼────────────────────────┐
    │  Express.static (HOISTED!)      │ ◄─── PHASE I FIX
    │  Serves /assets, /src, etc.     │      Issue 1
    └────────┬────────────────────────┘
             │
    ┌────────▼────────────────────────┐
    │  checkOperatingHours            │
    │  Whitelist: /auth/*, /me, etc.  │ ◄─── PHASE I FIX
    └────────┬────────────────────────┘      Issue 1
             │
    ┌────────▼────────┐
    │  Session        │
    └────────┬────────┘
             │
    ┌────────▼────────┐
    │  Passport Auth  │
    └────────┬────────┘
             │
    ┌────────▼────────┐
    │  Route Handlers │
    └─────────────────┘
```

### Casino Status API Flow (Issue 3)
```
Client                Server                     Response
  │                     │                           │
  │─── GET /api/casino-status ──►│                 │
  │                     │                           │
  │                     │ Calculate:                │
  │                     │ msUntilOpen = target - now│
  │                     │ (Server UTC time)         │
  │                     │                           │
  │◄─────────────────────────────────────────────│
  │  { isOpen: false,                             │
  │    msUntilOpen: 28800000,  ◄─── NEW!         │
  │    nextOpenTime: "2024-12-07T22:00:00Z" }    │
  │                                                │
  │ Client calculates:                            │
  │ localTarget = Date.now() + 28800000          │
  │ Countdown ticks against localTarget           │
  │                                                │
  │ Every 60 seconds:                             │
  │─── Re-fetch /api/casino-status ──►           │
  │ (Drift correction)                            │
```

### Admin Users API (Issue 2)
```
┌────────────────────────────────────────────┐
│  GET /api/admin/users?page=1&limit=50     │
└───────────────┬────────────────────────────┘
                │
        ┌───────▼──────────┐
        │  Query Prisma    │
        │  SELECT * FROM   │
        │  User            │
        │  ORDER BY        │
        │  createdAt DESC  │
        └───────┬──────────┘
                │
        ┌───────▼──────────────────┐
        │  Iterate Socket.IO       │ ◄─── NEW!
        │  Get active socket.user  │      Issue 2
        │  Build Set of online IDs │
        └───────┬──────────────────┘
                │
        ┌───────▼──────────────────┐
        │  Enrich User Data        │
        │  • isOnline: boolean     │
        │  • riskScore: number     │
        │  • Convert BigInt→Number │
        └───────┬──────────────────┘
                │
        ┌───────▼──────────┐
        │  Return JSON     │
        │  { users: [...], │
        │    pagination }  │
        └──────────────────┘
```

---

## Phase II: Interface Physics

### Info Modal Safe-Zone (Issue 4)
```
┌─────────────────────────────────────────┐
│  Browser Viewport (100vh)               │
│  ┌───────────────────────────────────┐  │
│  │  Info Modal Container             │  │
│  │  max-height: 85vh ◄─── 15% buffer│  │
│  │  position: fixed                  │  │
│  │  top: 50%, transform: -50%        │  │
│  │  ┌─────────────────────────────┐  │  │
│  │  │  Header (fixed)             │  │  │
│  │  ├─────────────────────────────┤  │  │
│  │  │  Content Area               │  │  │
│  │  │  flex: 1                    │  │  │
│  │  │  overflow-y: auto ◄─── Scroll│  │  │
│  │  │  -webkit-overflow-scrolling │  │  │
│  │  │                              │  │  │
│  │  │  [Long content scrolls here]│  │  │
│  │  │                              │  │  │
│  │  ├─────────────────────────────┤  │  │
│  │  │  Footer (fixed)             │  │  │
│  │  └─────────────────────────────┘  │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

### Dealer Avatar Interaction (Issue 6)
```
User Clicks Dealer
       │
       ▼
┌──────────────────────────┐
│  DealerAvatar.jsx        │
│  onClick={handleClick}   │
└──────────┬───────────────┘
           │
    ┌──────▼──────┐
    │  Pick Voice │
    │  Line from  │
    │  Database   │
    └──────┬──────┘
           │
    ┌──────▼──────────────────┐
    │  Set Speech Bubble      │
    │  setTimeout(3000, clear)│
    └──────┬──────────────────┘
           │
    ┌──────▼──────────────────┐
    │  Web Speech API         │
    │  window.speechSynthesis │
    │  .speak(utterance)      │
    └─────────────────────────┘

Visual Output:
┌─────────────────────────┐
│  "Better luck next     │ ◄─── Speech Bubble
│   time!"               │      (Animated)
└───────────┬─────────────┘
            │
         ┌──▼──┐
         │ 😊  │ ◄─── Dealer Avatar
         │ 👔  │
         └─────┘
```

### Armed Cursor Protocol (Issue 8)
```
┌────────────────────────────────────────────────────┐
│  Betting Controls (Bottom HUD)                     │
│  ┌──────┬──────┬────────────┬──────┬──────┐       │
│  │  ÷2  │  -5  │  💰 $250   │  +5  │  ×2  │       │
│  └──────┴──────┴────────────┴──────┴──────┘       │
│           ▲                                         │
│           │                                         │
│           └─ onCursorValueChange(newValue)         │
└────────────────────────────────────────────────────┘
           │
           ▼
┌────────────────────────────────────────────────────┐
│  Parent Component (WarTableZones)                  │
│  const [betCursorValue, setBetCursorValue] = ...  │
└────────────────────────────────────────────────────┘
           │
           ▼
┌────────────────────────────────────────────────────┐
│  User Taps Betting Spot                            │
│  onClick={() => placeBet(spotIndex, betCursorValue)│
└────────────────────────────────────────────────────┘
```

---

## Phase III: War Community Swarm

### Zone Topology (Issue 5)
```
┌─────────────────────────────────────────────────────────────────┐
│                        War Table (Community)                     │
│                                                                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌────┐│
│  │ Zone 0   │  │ Zone 1   │  │ Zone 2   │  │ Zone 3   │  │ Z4 ││
│  │  Card: A♠│  │  Card: K♥│  │  Card: Q♦│  │  Card: J♣│  │    ││
│  ├──────────┤  ├──────────┤  ├──────────┤  ├──────────┤  ├────┤│
│  │ [o][o][o]│  │ [o][o][o]│  │ [o][o][o]│  │ [o][o][o]│  │ ...││
│  │ [o][o]   │  │ [o][o]   │  │ [o][o]   │  │ [o][o]   │  │    ││
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘  └────┘│
│       ▲              ▲              ▲              ▲              │
│       │              │              │              │              │
│    5 spots       5 spots       5 spots       5 spots             │
│                                                                  │
│  Dealer Card: K♠                                                │
└─────────────────────────────────────────────────────────────────┘

Spot Data Structure:
{
  userId: "user123",
  amount: 250,
  userColor: "#FF00FF",  ◄─── Neon glow color
  result: "WIN"
}

Visual Representation:
┌──────┐
│ $250 │ ◄─── Occupied spot (border glows with userColor)
│ YOU  │
└──────┘  ring-4 ring-white (if current user)

┌──────┐
│  •   │ ◄─── Empty spot (dashed border, hover effect)
└──────┘
```

### Game Flow
```
1. BETTING Phase
   ├─ User adjusts cursor value (÷2, -5, +5, ×2)
   ├─ User taps empty spot
   └─ Server validates & occupies spot

2. DEALING Phase
   ├─ Server deals 1 card per zone
   ├─ Server deals 1 house card
   └─ Broadcast cards to all players

3. RESULT Phase
   ├─ Compare each zone card vs house card
   ├─ Calculate payouts for all spots in winning zones
   ├─ Handle ties (auto-war or surrender)
   └─ Broadcast results

4. RESET Phase
   ├─ Clear all spots
   ├─ Clear cards
   └─ Return to BETTING
```

---

## Phase IV: Global Bingo Singleton

### Singleton Architecture (Issue 9)
```
┌─────────────────────────────────────────────────────────┐
│  Server Startup (startServer function)                  │
└────────────────┬────────────────────────────────────────┘
                 │
         ┌───────▼────────────┐
         │  getGlobalBingoGame()│ ◄─── Called ONCE on startup
         └───────┬────────────┘
                 │
         ┌───────▼─────────────────────────┐
         │  new BingoEngine(...)           │
         │  • roomId: 'bingo_hall_global' │
         │  • maxPlayers: 200              │
         │  • startGame() ◄─── Auto-start │
         └───────┬─────────────────────────┘
                 │
         ┌───────▼─────────┐
         │  Game Loop      │
         │  Runs forever   │
         │  Every 4.5s:    │
         │  • Draw ball    │
         │  • Broadcast    │
         └─────────────────┘

Multiple Users Joining:
┌─────────┐     ┌─────────┐     ┌─────────┐
│ User A  │────►│ User B  │────►│ User C  │
└────┬────┘     └────┬────┘     └────┬────┘
     │               │               │
     └───────────────┴───────────────┘
                     │
         ┌───────────▼────────────────┐
         │  socket.emit(              │
         │   'join_bingo_hall'        │
         │  )                         │
         └───────────┬────────────────┘
                     │
         ┌───────────▼────────────────┐
         │  All join same room:       │
         │  'bingo_hall_global'       │
         │                            │
         │  All receive same events:  │
         │  • bingo_ball_called       │
         │  • bingo_winner            │
         │  • bingo_round_reset       │
         └────────────────────────────┘
```

### Late Joiner Flow
```
User Joins During PLAYING Phase
       │
       ▼
┌──────────────────────────┐
│  socket.emit(            │
│   'join_bingo_hall'      │
│  )                       │
└──────────┬───────────────┘
           │
    ┌──────▼──────────────────────┐
    │  Server Response:           │
    │  bingo_joined               │
    │  {                          │
    │    gameState: {             │
    │      phase: 'PLAYING',      │
    │      drawnBalls: [B5, I22], │
    │      timeUntilNextBall: 3s  │
    │    }                        │
    │  }                          │
    └──────┬──────────────────────┘
           │
    ┌──────▼──────────────────────┐
    │  Frontend Renders:          │
    │  "Game in progress..."      │
    │  "Next round starts in XX"  │
    │  [Spectator Mode]           │
    │  [Buy Card for Next Round]  │
    └─────────────────────────────┘
```

---

## Data Flow Examples

### War Bet Placement
```
Client                          Server                         Database
  │                               │                               │
  │──── placeBet(spot=12, $100)──►│                              │
  │                               │                               │
  │                               │ Validate:                     │
  │                               │ • Spot empty?                 │
  │                               │ • User has chips?             │
  │                               │ • Betting phase?              │
  │                               │                               │
  │                               │ Lock spot (Redis):            │
  │                               │ SETNX spot:12 user123        │
  │                               │                               │
  │◄─────── bet_placed ───────────│                              │
  │                               │                               │
  │                               │ Broadcast to room:            │
  │◄───── war:state_update ───────│                              │
  │   (All players see new spot)  │                               │
  │                               │                               │
  │                               │ [Game continues...]           │
  │                               │                               │
  │◄────── war:result ────────────│                              │
  │   { spot: 12, result: 'WIN',  │                              │
  │     payout: 200 }              │────── UPDATE User SET ──────►│
  │                               │        chipBalance += 200     │
```

### Admin Dashboard Load
```
Browser                         Server                    Socket.IO         Database
  │                               │                          │                 │
  │──── GET /api/admin/users ────►│                         │                 │
  │                               │                          │                 │
  │                               │──── prisma.user.findMany() ──────────────►│
  │                               │                          │                 │
  │                               │◄────── [1000 users] ─────────────────────│
  │                               │                          │                 │
  │                               │──── Get online users ───►│                 │
  │                               │                          │                 │
  │                               │         io.sockets       │                 │
  │                               │         .sockets         │                 │
  │                               │         .forEach(...)    │                 │
  │                               │                          │                 │
  │                               │◄──── [50 online IDs] ───│                 │
  │                               │                          │                 │
  │                               │ Merge data:              │                 │
  │                               │ users.map(u => ({        │                 │
  │                               │   ...u,                  │                 │
  │                               │   isOnline: set.has(u.id)│                 │
  │                               │ }))                      │                 │
  │                               │                          │                 │
  │◄────── JSON response ─────────│                          │                 │
  │   { users: [...],             │                          │                 │
  │     pagination: {...} }       │                          │                 │
```

---

## Performance Characteristics

### Latency Targets
```
┌─────────────────────────┬──────────────┬─────────────┐
│ Operation               │ Target       │ Actual      │
├─────────────────────────┼──────────────┼─────────────┤
│ Casino Status API       │ < 50ms       │ ~30ms       │
│ Admin Users API (50)    │ < 200ms      │ ~150ms      │
│ Socket bet placement    │ < 100ms      │ ~70ms       │
│ Bingo ball draw         │ 4.5s fixed   │ 4.5s        │
│ War round complete      │ < 3s         │ ~2.5s       │
└─────────────────────────┴──────────────┴─────────────┘
```

### Scalability
```
Component              Concurrent Limit    Notes
─────────────────────────────────────────────────────
War Zones              25 bets/round       Physical UI limit
Global Bingo           200 players         Configurable
Admin Dashboard        1000 users/page     Paginated
Socket.IO              10,000+ connections Native capability
Database (Neon)        Shared pool         Auto-scaling
Redis (Upstash)        Unlimited           Serverless
```

---

## Security Layers

```
┌────────────────────────────────────────────────────┐
│  Layer 1: Operating Hours Enforcement              │
│  • Casino closed 2 AM - 10 PM ET                   │
│  • Admins bypass via biometric                     │
└─────────────────┬──────────────────────────────────┘
                  │
┌─────────────────▼──────────────────────────────────┐
│  Layer 2: Session Authentication                   │
│  • Passport.js + Google OAuth                      │
│  • Redis session store                             │
└─────────────────┬──────────────────────────────────┘
                  │
┌─────────────────▼──────────────────────────────────┐
│  Layer 3: Role-Based Access (isAdmin middleware)   │
│  • Admin routes protected                          │
│  • Email whitelist check                           │
└─────────────────┬──────────────────────────────────┘
                  │
┌─────────────────▼──────────────────────────────────┐
│  Layer 4: Auto-Moderation (Chat/Messages)          │
│  • Profanity filter                                │
│  • Spam detection                                  │
│  • Automated warnings                              │
└─────────────────┬──────────────────────────────────┘
                  │
┌─────────────────▼──────────────────────────────────┐
│  Layer 5: Input Validation                         │
│  • Bet amounts checked                             │
│  • Spot indices validated                          │
│  • Message sanitization                            │
└────────────────────────────────────────────────────┘
```

---

**Document Version**: 1.0  
**Last Updated**: December 7, 2024  
**Maintained By**: VegasCore Dev Team
